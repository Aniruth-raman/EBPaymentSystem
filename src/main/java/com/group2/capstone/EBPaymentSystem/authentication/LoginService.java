package com.group2.capstone.EBPaymentSystem.authentication;

public class LoginService {
    public boolean login(String username, String password) {
        // User user = DBService.getUserByuserName(username);
        // if (user) {
        //     String encryptedPassword = EncryptionService.hashPassword(password); 
        //     user.getPassword().equals(encryptedPassword);
        // }
        // 
        // retrieve the user from the database using the given username
        // if the user exists, hash the given password using EncryptionService.hashPassword()
        // compare the resulting hash with the stored hash
        // if they match, return true (login successful)
        // otherwise, return false (login failed)
        return false;
    }
}
